[project]
name = "rag-citation-starter"
version = "0.1.0"
description = "Dependency-light source-cited retrieval starter for grounded RAG workflows"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
pythonpath = ["src"]
